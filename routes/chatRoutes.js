/*const express = require('express'),
    { getMessage, postMessage, getMessageByUser, delMessage } = require('../controller/chat.js'),
    { auth } = require('../helpers/auth.js'),
    chatRouter = express.Router();

// get messages route
chatRouter.get('/messages',auth, getMessage);

// post messages route
chatRouter.post('/newmessages',auth, postMessage);

// get messages by reciever route
chatRouter.get('/messagesByUser/:user/:receiver', getMessageByUser);

// delete single message
chatRouter.delete('/deletemessages/:sender',auth, delMessage);


exports.chatRouter = chatRouter;
*/